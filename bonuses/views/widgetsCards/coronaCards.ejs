<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
<link rel="stylesheet" type="text/css" href="/css/cards.css" />


<div class="page-content page-container" id="page-contentCorona">
    <div class="padding">
        <div class="row container d-flex justify-content-center">
            <div class="col-sm-12">
                <!-- Draggable default card start -->
                <div class="card">
                    <div class="card-header">
                        <h5>Widgets Corona Cases</h5>
                    </div>
                    <div class="card-block">
                        <div class="row" id="corona">
                            <!-- HERE IS THE JAVASCRIPT CODE -->
                        </div>
                    </div>
                </div> <!-- Draggable default card start -->
            </div>
        </div>
    </div>
</div>

<script>
$( document ).ready(function() {
    $("#corona").sortable();
    $("#corona").disableSelection();
});
</script>

<script>

  
    var addColsCorona = function (num){
        <% for(var i = 0; i < data.corona.length; i++) { %>
            var myCol = $('<div class="col-lg-12 col-xl-3" style=""></div>');
            var myIcon = $('<div class="card-sub"> <img  src="<%= data.corona[i].corona.flag%>" alt="Card image cap", width="100">')
            var myDeleteBtn = $('<form class="form-signin" action="/widgets" method="POST"> <input type="hidden" id="coronaClose" name="coronaClose" value="<%=data.corona[i].countryName%>">  <button type="submit" class="close" aria-label="Close"> <span aria-hidden="true">&times;</span> </button> </form>')
            var myBlock = $('<div class="card-block"></div>');
            var myTitle = $('<h4 class="card-title"><%=data.corona[i].countryName%></h4>');
            var myBody = $('<p class="card-text">Corona cases in <%=data.corona[i].countryName%>  are : <%= data.corona[i].corona.cases %> and <%= data.corona[i].corona.recovered %> recovered </p>');
            var myEditButton = $('<form class="form-signin" action="/widgets" method="POST"> <label for="EditCorona">Country</label> <input type="adress" class="form-control" id="EditCorona" name="EditCorona" placeholder="Country"> <input type="hidden" id="currentCountry" name="currentCountry" value="<%=data.corona[i].countryName%>">  <button type="submit" class="btn btn-primary">Submit</button> </form>');
            myDeleteBtn.appendTo(myBlock);
            myIcon.appendTo(myBlock);
            myTitle.appendTo(myBlock);
            myBody.appendTo(myBlock);
            myBlock.appendTo(myCol);
            myEditButton.appendTo(myCol);
            myCol.appendTo('#corona');
        <% } %>
        
    };

    $(document).ready(function(){
        addColsCorona(<%= data.corona.length%>);
        return false;
    });

</script>