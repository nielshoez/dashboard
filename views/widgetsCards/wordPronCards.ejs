<script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
<link rel="stylesheet" type="text/css" href="/css/cards.css" />


<div class="page-content page-container" id="page-contentWordPron">
    <div class="padding">
        <div class="row container d-flex justify-content-center">
            <div class="col-sm-12">
                <!-- Draggable default card start -->
                <div class="card">
                    <div class="card-header">
                        <h5>Widgets Word</h5>
                    </div>
                    <div class="card-block">
                        <div class="row" id="wordPronDEf"> 
                            <!-- HERE IS THE JAVASCRIPT CODE -->
                        </div>
                    </div>
                </div> <!-- Draggable default card start -->
            </div>
        </div>
    </div>
</div>

<script>
$( document ).ready(function() {
    $("#wordPronDEf").sortable();
    $("#wordPronDEf").disableSelection();
});
</script>

<script>
    var addColswordPron = function (num){
        <% for(var i = 0; i < data.wordPron.length; i++) { %>
            var myCol = $('<div class="col-lg-12 col-xl-3" style=""></div>');
            var myDeleteBtn = $('<form class="form-signin" action="/widgets" method="POST"> <input type="hidden" id="wordPronClose" name="wordPronClose" value="<%=data.wordPron[i].word%>">  <button type="submit" class="close" aria-label="Close"> <span aria-hidden="true">&times;</span> </button> </form>')
            var myBlock = $('<div class="card-block"></div>');
            var myTitle = $('<h4 class="card-title"><%=data.wordPron[i].word%></h4>');
            var myBody = $('<p class="card-text">the pronunciation of <%=data.wordPron[i].word%>  is <br> <%= data.wordPron[i].pron.phone %> and it sounds like :  </p>');
            var myAudio = $('<p> <audio controls width="100">  <source src="<%= data.wordPron[i].pron.audio %>" type="audio/mpeg"></audio> </p>')
            var myEditButton = $('<form class="form-signin" action="/widgets" method="POST"> <label for="EditwordPron">WordPron</label> <input type="adress" class="form-control" id="EditwordPron" name="EditwordPron" placeholder="WordPron"> <input type="hidden" id="currentWordPron" name="currentWordPron" value="<%=data.wordPron[i].word%>">  <button type="submit" class="btn btn-primary">Submit</button> </form>');
            myDeleteBtn.appendTo(myBlock);
            myTitle.appendTo(myBlock);
            myBody.appendTo(myBlock);
            myAudio.appendTo(myBlock);
            myBlock.appendTo(myCol);
            myEditButton.appendTo(myCol);
            myCol.appendTo('#wordPronDEf');
        <% } %>
        
    };

    $(document).ready(function(){
        addColswordPron(<%= data.wordPron.length%>);
        return false;
    });

</script>